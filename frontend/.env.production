# Production environment variables for Vercel
# Replace the URL below with your actual Render backend URL after deploying it.
# This file is committed to git (it has no secrets). The VITE_API_URL value
# is also set in the Vercel dashboard, which takes precedence over this file.
VITE_API_URL=https://buildgram-backend.onrender.com
